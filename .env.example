NOCTRA_CONFIG_POLICY=customizable

# CI-only: disables Electron setuid sandbox for headless Linux runners.
# Does NOT disable the namespace sandbox (process isolation + seccomp remain active).
# Set to 1 in CI environments where the chrome-sandbox binary lacks SUID permissions.
CI=false
CI_DISABLE_SETUID_SANDBOX=0
